spring:
  application:
    name: enterprise-inbox-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/inbox
    username: inbox
    password: inbox
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080
